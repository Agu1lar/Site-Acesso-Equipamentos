#cloud-config
package_update: true
package_upgrade: true
packages:
  - docker.io
  - docker-compose-v2
  - ufw
runcmd:
  - ufw allow OpenSSH
  - ufw --force enable
  - systemctl enable --now docker
  - usermod -aG docker ubuntu || true
